# На VPS: скопируй в .env.production и поставь ТОТ ЖЕ порт, что в nginx proxy_pass
#   cp .env.production.example .env.production
#   nano .env.production
#
# Пример: если в nginx  proxy_pass http://127.0.0.1:3045;  →  PORT=3045

PORT=3045
