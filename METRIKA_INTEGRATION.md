# Интеграция с Яндекс.Метрикой

## ⚠️ Обязательно для работы

**Сначала открой:** [docs/CONVERSIONS_DEPLOY_REQUIREMENTS.md](docs/CONVERSIONS_DEPLOY_REQUIREMENTS.md) — там полный чеклист. Без БД и токена ничего не заработает.

- **leads**: в Cloud задан **DATABASE_URL** (та же БД с таблицей leads).
- **metrika-conversion**: в Cloud задан **YANDEX_METRIKA_TOKEN** (OAuth-токен Метрики).

---

## ✅ Что реализовано

Полная автоматическая интеграция отправки целей в Яндекс.Метрику при изменении статусов заявок.

## 🎯 Как это работает

### 1. Отслеживание пользователей

**Из сайта:**
- При загрузке сайта Метрика автоматически присваивает ClientID
- При отправке формы ClientID сохраняется в БД в поле `ym_client_id`

**Из Telegram (заявки без визита на сайт):**
- Используется UserID вида `telegram_{lead_id}` при загрузке офлайн-конверсии

### 2. Автоматическая отправка целей (офлайн-конверсии по API)
Конверсии передаются в Метрику **официальным способом**: загрузка CSV через `POST /management/v1/counter/.../offline_conversions/upload`. В CSV передаются: ClientId или UserId, Target (ид цели), DateTime (Unix), Price, Currency.

#### Через Telegram-бота (кнопки в админке)
Когда нажимаете кнопку в Telegram:
- ✅ Записался на пробное → цель `trial_scheduled`
- 🎓 Записался на обучение → цель `enrolled`
- 🤔 Думает → цель `considering`
- ❌ Нецелевой → цель `rejected`

#### Через веб-админку
Когда меняете статус в админ-панели:
- `trial_scheduled` → цель `trial_scheduled`
- `trial_completed` → цель `trial_completed`
- `enrolled` → цель `enrolled`
- `paid` → цель `payment_completed`

## 📊 Настройка целей в Яндекс.Метрике

1. Зайдите в Яндекс.Метрику (счётчик 104854671)
2. Перейдите в **Настройки** → **Цели**
3. Создайте JavaScript-события со следующими идентификаторами:

| Название цели | Идентификатор | Описание |
|--------------|---------------|----------|
| Запись на пробное занятие | `trial_scheduled` | Клиент записался на пробное |
| Прошёл пробное занятие | `trial_completed` | Клиент посетил пробное |
| Запись на курс | `enrolled` | Клиент записался на полный курс |
| Оплата курса | `payment_completed` | Клиент оплатил обучение |
| Раздумывает | `considering` | Клиент в процессе принятия решения |
| Нецелевая заявка | `rejected` | Нецелевое обращение |

## 🔧 Технические детали

### Отправка офлайн-конверсий
`backend/metrika-conversion/index.py` — формирует CSV (ClientId/UserId, Target, DateTime, Price, Currency) и загружает его через официальный API Метрики (`offline_conversions/upload`). При смене статуса заявки (из Telegram или админки) вызывается функция **leads**, которая дергает **metrika-conversion** с параметром `goal`. В Метрике должны быть созданы цели типа «JavaScript-событие» с идентификаторами точно: `trial_scheduled`, `trial_completed`, `enrolled`, `payment_completed`.

**Требования:** В облачной функции **metrika-conversion** задан секрет `YANDEX_METRIKA_TOKEN` (OAuth-токен владельца счётчика). В Метрике созданы цели типа «JavaScript-событие» с идентификаторами: `trial_scheduled`, `trial_completed`, `enrolled`, `payment_completed` (и при необходимости `TARGET_CLIENT` для ручной отправки из админки).

### Автоматический триггер в backend/leads
При изменении статуса заявки (PUT request) автоматически вызывается `send_metrika_goal()`

### Передача контекста
Вместе с целью отправляются дополнительные параметры:
- `lead_id` - ID заявки в БД
- `source` - источник (website, telegram, admin)
- `course` - выбранный курс (acting/oratory)
- `status` - текущий статус заявки

## 🎬 Пример работы

### Сценарий 1: Заявка с сайта
1. Пользователь заходит на actor-school.ru
2. Метрика присваивает ClientID `987654321`
3. Пользователь заполняет форму → ClientID сохраняется в БД
4. Вы меняете статус в админке на "trial_scheduled"
5. ✅ Автоматически отправляется цель с ClientID `987654321`
6. В Метрике видно с какой рекламы пришла заявка

### Сценарий 2: Заявка из Telegram
1. Пользователь пишет в бота → создаётся заявка (без визита на сайт)
2. Вы в Telegram нажимаете "✅ Записался на пробное"
3. ✅ В Метрику загружается офлайн-конверсия с UserID `telegram_42` и целью `trial_scheduled`
4. В Метрике конверсия учитывается (привязка к рекламе — по yclid/источникам, если есть)

## 🔍 Отладка

Проверить отправку целей можно через логи функций:
```bash
# В логах увидите
✅ Цель 'trial_scheduled' отправлена в метрику для lead 42
```

## 🚀 Преимущества решения

✅ **Автоматически** - не нужно ничего делать вручную
✅ **Централизованно** - одна функция для всех источников
✅ **Трекинг** - отслеживание эффективности каждого канала
✅ **Гибкость** - легко добавить новые цели
✅ **Надёжность** - если отправка не удалась, не ломает основную логику

## 👀 Как проверить, что офлайн-конверсии работают

### Что сделать в Метрике один раз (подключение)

Отдельного «подключения» для API нет: загрузки через API попадают в тот же раздел, что и ручные. Нужно только создать цели.

1. **Создать цели (обязательно)**  
   **Настройка** → **Цели** → **Добавить цель** → тип **«JavaScript-событие»**.  
   Создайте цели с идентификаторами **точно так** (без пробелов и лишних символов):
   - `trial_scheduled`
   - `trial_completed`
   - `enrolled`
   - `payment_completed`  
   Название цели может быть любым (например: «Запись на пробное», «Оплата»).

2. **Ничего больше в интерфейсе подключать не нужно**  
   Загрузка идёт через API с бэкенда. Кнопка «Загрузить данные» в Метрике — для ручной загрузки CSV; наши конверсии туда попадают автоматически тем же способом (тот же API).

### Где смотреть, что загрузки дошли

- **Настройка** → вкладка **«Загрузка данных»** → блок **«Загрузка офлайн-конверсий»**.  
  Там есть **история загрузок**: дата, комментарий, статус (Загружено → Обработано и т.д.).  
  Если наша функция отработала без ошибки, через некоторое время там появится новая строка (комментарий мы передаём, например: «Target client conversion - acting»).
- Обработка может занимать **до 2 часов** (иногда до 24). Статус сменится с загрузки на обработку/привязку.

### Где смотреть конверсии в отчётах

- **Отчёты** → **Стандартные отчёты** → **Конверсии** — выберите нужную цель (trial_scheduled, enrolled и т.д.).
- Там же можно смотреть по источникам, кампаниям, датам.

### Как быстро убедиться, что «всё уехало»

1. Поменяй статус заявки в Telegram или в админке (например на «Записался на пробное»).
2. Через 1–2 минуты зайди в **Настройка** → **Загрузка данных** и проверь, появилась ли новая загрузка с сегодняшней датой и нашим комментарием.
3. Если загрузка есть и без ошибки — конверсия ушла. В отчётах по целям она появится после обработки (обычно в течение 2 часов).

Если загрузки нет — смотри логи облачной функции **metrika-conversion** в Yandex Cloud: там будет ответ API Метрики (например «goal not found», если цель с таким идентификатором не создана).

---

## 📈 Что можно анализировать

После настройки вы сможете видеть в Метрике:
- Откуда приходят заявки (из каких рекламных каналов)
- Какой % доходит до пробного занятия
- Какой % записывается на курс
- ROI каждого рекламного канала
- Стоимость привлечения клиента (CAC)
